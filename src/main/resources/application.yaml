spring:
  application:
    name: member-api
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health, info
  info:
    env:
      enabled: true

info:
  team-name: ${TEAM_NAME:default}
